version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: square-ui-postgres-prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: square_ui
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  home:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-home:latest
    container_name: square-ui-home-prod
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3000:3000"
    restart: unless-stopped

  calendar:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-calendar:latest
    container_name: square-ui-calendar-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3001:3000"
    restart: unless-stopped

  chat:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-chat:latest
    container_name: square-ui-chat-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3002:3000"
    restart: unless-stopped

  emails:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-emails:latest
    container_name: square-ui-emails-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3003:3000"
    restart: unless-stopped

  dashboard-1:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-dashboard-1:latest
    container_name: square-ui-dashboard-1-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3004:3000"
    restart: unless-stopped

  task-management:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-task-management:latest
    container_name: square-ui-task-management-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3005:3000"
    restart: unless-stopped

  projects-timeline:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-projects-timeline:latest
    container_name: square-ui-projects-timeline-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3006:3000"
    restart: unless-stopped

  dashboard-2:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-dashboard-2:latest
    container_name: square-ui-dashboard-2-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3007:3000"
    restart: unless-stopped

  dashboard-3:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-dashboard-3:latest
    container_name: square-ui-dashboard-3-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3008:3000"
    restart: unless-stopped

  dashboard-4:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-dashboard-4:latest
    container_name: square-ui-dashboard-4-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3009:3000"
    restart: unless-stopped

  employees:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-employees:latest
    container_name: square-ui-employees-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3010:3000"
    restart: unless-stopped

  files:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-files:latest
    container_name: square-ui-files-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3011:3000"
    restart: unless-stopped

  leads:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-leads:latest
    container_name: square-ui-leads-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3012:3000"
    restart: unless-stopped

  payrolls:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-payrolls:latest
    container_name: square-ui-payrolls-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3013:3000"
    restart: unless-stopped

  tasks:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-tasks:latest
    container_name: square-ui-tasks-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3014:3000"
    restart: unless-stopped

  dashboard-3-baseui:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-dashboard-3-baseui:latest
    container_name: square-ui-dashboard-3-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3015:3000"
    restart: unless-stopped

  dashboard-4-baseui:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-dashboard-4-baseui:latest
    container_name: square-ui-dashboard-4-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3016:3000"
    restart: unless-stopped

  emails-baseui:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-emails-baseui:latest
    container_name: square-ui-emails-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3017:3000"
    restart: unless-stopped

  files-baseui:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-files-baseui:latest
    container_name: square-ui-files-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3018:3000"
    restart: unless-stopped

  leads-baseui:
    image: ghcr.io/${IMAGE_PREFIX}/square-ui-leads-baseui:latest
    container_name: square-ui-leads-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3019:3000"
    restart: unless-stopped

volumes:
  postgres_data_prod:
