version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: square-ui-postgres-prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: square_ui
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  home:
    build:
      context: ./home
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-home-prod
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3000:3000"
    restart: unless-stopped

  calendar:
    build:
      context: ./templates/calendar
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-calendar-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3001:3000"
    restart: unless-stopped

  chat:
    build:
      context: ./templates/chat
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-chat-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3002:3000"
    restart: unless-stopped

  emails:
    build:
      context: ./templates/emails
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-emails-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3003:3000"
    restart: unless-stopped

  dashboard-1:
    build:
      context: ./templates/dashboard-1
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-dashboard-1-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3004:3000"
    restart: unless-stopped

  task-management:
    build:
      context: ./templates/task-management
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-task-management-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3005:3000"
    restart: unless-stopped

  projects-timeline:
    build:
      context: ./templates/projects-timeline
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-projects-timeline-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3006:3000"
    restart: unless-stopped

  dashboard-2:
    build:
      context: ./templates/dashboard-2
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-dashboard-2-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3007:3000"
    restart: unless-stopped

  dashboard-3:
    build:
      context: ./templates/dashboard-3
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-dashboard-3-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3008:3000"
    restart: unless-stopped

  dashboard-4:
    build:
      context: ./templates/dashboard-4
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-dashboard-4-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3009:3000"
    restart: unless-stopped

  employees:
    build:
      context: ./templates/employees
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-employees-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3010:3000"
    restart: unless-stopped

  files:
    build:
      context: ./templates/files
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-files-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3011:3000"
    restart: unless-stopped

  leads:
    build:
      context: ./templates/leads
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-leads-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3012:3000"
    restart: unless-stopped

  payrolls:
    build:
      context: ./templates/payrolls
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-payrolls-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3013:3000"
    restart: unless-stopped

  tasks:
    build:
      context: ./templates/tasks
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-tasks-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3014:3000"
    restart: unless-stopped

  dashboard-3-baseui:
    build:
      context: ./templates-baseui/dashboard-3
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-dashboard-3-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3015:3000"
    restart: unless-stopped

  dashboard-4-baseui:
    build:
      context: ./templates-baseui/dashboard-4
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-dashboard-4-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3016:3000"
    restart: unless-stopped

  emails-baseui:
    build:
      context: ./templates-baseui/emails
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-emails-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3017:3000"
    restart: unless-stopped

  files-baseui:
    build:
      context: ./templates-baseui/files
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-files-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3018:3000"
    restart: unless-stopped

  leads-baseui:
    build:
      context: ./templates-baseui/leads
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: square-ui-leads-baseui-prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: production
    ports:
      - "3019:3000"
    restart: unless-stopped

volumes:
  postgres_data_prod:
