version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: square-ui-postgres-dev
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: square_ui
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  home:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NODE_ENV: development
    container_name: square-ui-home-dev
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3000:3000"
    restart: unless-stopped

  calendar:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/calendar
    container_name: square-ui-calendar-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3001:3000"
    restart: unless-stopped

  chat:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/chat
    container_name: square-ui-chat-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3002:3000"
    restart: unless-stopped

  emails:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/emails
    container_name: square-ui-emails-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3003:3000"
    restart: unless-stopped

  dashboard-1:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/dashboard-1
    container_name: square-ui-dashboard-1-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3004:3000"
    restart: unless-stopped

  task-management:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/task-management
    container_name: square-ui-task-management-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3005:3000"
    restart: unless-stopped

  projects-timeline:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/projects-timeline
    container_name: square-ui-projects-timeline-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3006:3000"
    restart: unless-stopped

  dashboard-2:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/dashboard-2
    container_name: square-ui-dashboard-2-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3007:3000"
    restart: unless-stopped

  dashboard-3:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/dashboard-3
    container_name: square-ui-dashboard-3-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3008:3000"
    restart: unless-stopped

  dashboard-4:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/dashboard-4
    container_name: square-ui-dashboard-4-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3009:3000"
    restart: unless-stopped

  employees:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/employees
    container_name: square-ui-employees-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3010:3000"
    restart: unless-stopped

  files:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/files
    container_name: square-ui-files-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3011:3000"
    restart: unless-stopped

  leads:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/leads
    container_name: square-ui-leads-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3012:3000"
    restart: unless-stopped

  payrolls:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/payrolls
    container_name: square-ui-payrolls-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3013:3000"
    restart: unless-stopped

  tasks:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/tasks
    container_name: square-ui-tasks-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3014:3000"
    restart: unless-stopped

  bookmarks:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/bookmarks
    container_name: square-ui-bookmarks-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3020:3000"
    restart: unless-stopped

  dashboard-5:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/dashboard-5
    container_name: square-ui-dashboard-5-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3021:3000"
    restart: unless-stopped

  habit-tracker:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/habit-tracker
    container_name: square-ui-habit-tracker-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3022:3000"
    restart: unless-stopped

  maps:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/maps
    container_name: square-ui-maps-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3023:3000"
    restart: unless-stopped

  marketing-dashboard:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/marketing-dashboard
    container_name: square-ui-marketing-dashboard-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3024:3000"
    restart: unless-stopped

  rentals:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates/rentals
    container_name: square-ui-rentals-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3025:3000"
    restart: unless-stopped

  dashboard-3-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/dashboard-3
    container_name: square-ui-dashboard-3-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3015:3000"
    restart: unless-stopped

  dashboard-4-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/dashboard-4
    container_name: square-ui-dashboard-4-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3016:3000"
    restart: unless-stopped

  emails-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/emails
    container_name: square-ui-emails-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3017:3000"
    restart: unless-stopped

  files-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/files
    container_name: square-ui-files-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3018:3000"
    restart: unless-stopped

  leads-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/leads
    container_name: square-ui-leads-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3019:3000"
    restart: unless-stopped

  bookmarks-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/bookmarks
    container_name: square-ui-bookmarks-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3026:3000"
    restart: unless-stopped

  calendar-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/calendar
    container_name: square-ui-calendar-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3027:3000"
    restart: unless-stopped

  dashboard-5-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/dashboard-5
    container_name: square-ui-dashboard-5-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3028:3000"
    restart: unless-stopped

  habit-tracker-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/habit-tracker
    container_name: square-ui-habit-tracker-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3029:3000"
    restart: unless-stopped

  maps-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/maps
    container_name: square-ui-maps-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3030:3000"
    restart: unless-stopped

  marketing-dashboard-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/marketing-dashboard
    container_name: square-ui-marketing-dashboard-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3031:3000"
    restart: unless-stopped

  rentals-baseui:
    build:
      context: .
      dockerfile: Dockerfile.template.dev
      args:
        TEMPLATE_PATH: templates-baseui/rentals
    container_name: square-ui-rentals-baseui-dev
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NODE_ENV: development
    ports:
      - "3032:3000"
    restart: unless-stopped

volumes:
  postgres_data_dev:
