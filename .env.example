# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL connection string for all services
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/square_ui?schema=public"

# PostgreSQL credentials (used by Docker container)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# Public URL where your application will be accessed
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node environment (development/production)
NODE_ENV=development

# ============================================================================
# DEPLOYMENT CONFIGURATION (GitHub Actions Secrets)
# ============================================================================
# These are NOT used locally, set them as GitHub Secrets instead

# Production Deployment
# PROD_DEPLOY_HOST=your-server-ip-or-domain
# PROD_DEPLOY_USER=ssh-user
# PROD_DEPLOY_KEY=-----BEGIN PRIVATE KEY-----...
# PROD_DEPLOY_PORT=22
# PROD_DATABASE_URL=postgresql://user:password@host:5432/square_ui
# PROD_APP_URL=https://your-domain.com

# Development Deployment
# DEV_DEPLOY_HOST=your-dev-server-ip
# DEV_DEPLOY_USER=ssh-user
# DEV_DEPLOY_KEY=-----BEGIN PRIVATE KEY-----...
# DEV_DEPLOY_PORT=22
# DEV_DATABASE_URL=postgresql://user:password@host:5432/square_ui_dev
# DEV_APP_URL=https://dev.your-domain.com
