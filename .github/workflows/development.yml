name: Development Deployment

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        template:
          - bookmarks
          - calendar
          - chat
          - dashboard-1
          - dashboard-2
          - dashboard-3
          - dashboard-4
          - dashboard-5
          - emails
          - employees
          - files
          - habit-tracker
          - leads
          - maps
          - marketing-dashboard
          - payrolls
          - projects-timeline
          - rentals
          - task-management
          - tasks

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Enable pnpm
        run: corepack enable pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        working-directory: templates/${{ matrix.template }}

      - name: Run linting
        run: pnpm lint
        working-directory: templates/${{ matrix.template }}
        continue-on-error: true

  deploy:
    runs-on: ubuntu-latest
    needs: lint-and-test
    if: github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to development
        env:
          DATABASE_URL: ${{ secrets.DEV_DATABASE_URL }}
          NEXT_PUBLIC_APP_URL: ${{ secrets.DEV_APP_URL }}
          DEPLOY_HOST: ${{ secrets.DEV_DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEV_DEPLOY_USER }}
          DEPLOY_KEY: ${{ secrets.DEV_DEPLOY_KEY }}
          DEPLOY_PORT: ${{ secrets.DEV_DEPLOY_PORT }}
        run: |
          mkdir -p ~/.ssh
          echo "$DEPLOY_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${DEPLOY_PORT:-22} "$DEPLOY_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

          ssh -i ~/.ssh/deploy_key -p ${DEPLOY_PORT:-22} "$DEPLOY_USER@$DEPLOY_HOST" << EOF
          set -e
          mkdir -p /home/thim/websites
          if [ ! -d /home/thim/websites/square-ui/.git ]; then
            git clone https://github.com/Thimote147/square-ui.git /home/thim/websites/square-ui
          fi
          cd /home/thim/websites/square-ui
          export DATABASE_URL="$DATABASE_URL"
          export NEXT_PUBLIC_APP_URL="$NEXT_PUBLIC_APP_URL"
          git fetch origin dev
          git reset --hard origin/dev
          docker compose -f docker-compose.dev.yml up -d --build
          docker compose -f docker-compose.dev.yml exec -T home pnpm prisma migrate deploy || true
          EOF

      - name: Notify deployment status
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const status = '${{ job.status }}';
            const message = status === 'success'
              ? '✅ Development deployment successful!'
              : '❌ Development deployment failed!';
            console.log(message);
